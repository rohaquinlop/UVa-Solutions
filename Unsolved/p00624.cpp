/*
 *
 * Problema UVa 624 - CD
 *
*/

#include <iostream>
#include <cstdio>
#include <vector>

using namespace std;

vector<int> backTracking(vector<int>& vec, int i, int acum, int& val){
    if(   )
}

int main(){
    int n, t, aux;
    int i;

    while( scanf("%d", &n) == 1  ){
        scanf("%d", &t);
        vector<int> nums;

        for(i = 0; i < t; i++){
            scanf("%d", &aux);
            nums.push_back(aux);
        }
    }


    return 0;
}
